#include <stdio.h>
	
	int pascal(int l, int c);
	int i, l, c;	{	
		printf("Entre com o número de linhas e colunas desejadas para que o Triângulo de Pascal seja formado: \n");
		scanf("%d %d", &l, &c);
		
		for(i=1, j=1; i<=l, i<=l; i++, j++){
			{
			if(l==1 && c==1)
			printf("1");
			}
		printf(



int main(){

	pascal(
